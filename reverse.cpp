#include <iostream>
#include <string>
using namespace std;
   
    void reverse (const string& word){
        string reversed = "";
        int length = word.size();
        for (int i = 0; i < length; i--)
        {
           reversed += word[i];
         
        }
        
    }

    
int main(){
    string word;
    cout<<"enter word";
    cin>>word;
   void reverse ();
   
}